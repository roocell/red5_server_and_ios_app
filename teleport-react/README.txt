// good explanation of react here
// https://www.raywenderlich.com/126063/react-native-tutorial


// base project IDE and stuff
1. install atom
2. install nuclide in atom
3. follow other nuclide instructions w.r.t packages
4. install flow (default react project uses it) flowtype.org
    npm install --save-dev flow-bin



// DRAWER
// https://github.com/root-two/react-native-drawer
// npm i --save react-native-drawer
// demo code (which I copied) requires
// npm i --save react-native-slider
